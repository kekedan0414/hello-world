**【rocket】**
		2016年阿里贡献给apache，目前版本是4.7.1。公司使用版本3.5.8，目前升级到4.5.1。

**【如何保证消息幂等性】**
		生产者发送给MQ，MQ发消息给消费者，如果出现网络抖动，触发重发机制，可能导致消息重复。
		解决的方法就是给消息生成一个全局唯一的MsgID，在消费的时候，消费成功的话就将MsgId放入redis或者数据库。然后消费的时候再去redis或数据库里找去重，看是否消费过。有的话直接跳过。

